# ==================================================
# WP-Sync 環境設定ファイル
# ==================================================
# このファイルを編集して、本番環境とステージング環境の接続情報を設定してください。


# --------------------------------------------------
# 本番環境情報
# --------------------------------------------------

# 本番環境のドメイン（例: example.com）
PRD_DOMAIN=""

# SSH接続先（例: user@example.com）
PRD_SSH_DESTINATION=""

# SSHポート番号（例: 10022）
PRD_SSH_PORT=""

# WordPressのルートディレクトリ（例: /home/user/example.com/public_html）
PRD_PUBLIC_DIR_PATH=""
PRD_PUBLIC_DIR_PATH_WITH_DESTINATION="${PRD_SSH_DESTINATION}:${PRD_PUBLIC_DIR_PATH}"

# データベース接続情報
PRD_DB_HOST=""           # 例: localhost または mysql.example.com
PRD_DB_NAME=""           # 例: wp_production
PRD_DB_USER=""           # 例: db_user
PRD_DB_PASSWORD=""       # 例: db_password


# --------------------------------------------------
# ステージング環境情報
# --------------------------------------------------

# ステージング環境のドメイン（例: stg.example.com）
STG_DOMAIN=""

# SSH接続先（本番と同じサーバーの場合は同じ値）
STG_SSH_DESTINATION=""

# SSHポート番号
STG_SSH_PORT=""

# WordPressのルートディレクトリ（例: /home/user/example.com/public_html/stg.example.com）
STG_PUBLIC_DIR_PATH=""
STG_PUBLIC_DIR_PATH_WITH_DESTINATION="${STG_SSH_DESTINATION}:${STG_PUBLIC_DIR_PATH}"

# データベース接続情報
STG_DB_HOST=""           # 例: localhost
STG_DB_NAME=""           # 例: wp_staging
STG_DB_USER=""           # 例: db_user
STG_DB_PASSWORD=""       # 例: db_password

# Basic認証用.htpasswdのコピー先（通常は変更不要）
HTPASSWD_PATH_WITH_DESTINATION="${STG_SSH_DESTINATION}:"


# --------------------------------------------------
# バックアップ設定（通常は変更不要）
# --------------------------------------------------

# 本番環境のバックアップ保存先
PRD_DB_DUMP_FILE_PATH="./.wp-sync/tmp/prd_dump.sql"
PRD_FILE_BACKUP_DIR_PATH="./.wp-sync/tmp/prd_public_html"

# ステージング環境のバックアップ保存先
STG_DB_DUMP_FILE_PATH="./.wp-sync/tmp/stg_dump.sql"
STG_FILE_BACKUP_DIR_PATH="./.wp-sync/tmp/stg_public_html"


# --------------------------------------------------
# 同期設定
# --------------------------------------------------

# 同期対象外のパス（カンマ区切り）
# 例: wp-content/cache,wp-content/uploads/backwpup-*-logs
EXCLUDES=""

# ステージング環境では無効化するプラグイン（カンマ区切り）
# 例: wordfence,redis-cache,wp-optimize,autoptimize
PLUGINS=""
